# Keep the Python plan from requirements.txt; only add the Node toolchain here.
# Using `providers = ["...", "node"]` made the build stage Node-first and dropped Python from PATH.
[phases.setup]
nixPkgs = ["...", "nodejs"]

[variables]
NIXPACKS_NODE_VERSION = "20"
